export const metadata = {
  title: 'Citale | Sign Up',
  description: 'Thank you for joining Citale!',
};

import '../../globals.css';

export default function SignupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
